{
  "name": "bezelapp",
  "version": "0.0.1",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:ext": "yarn workspace ext dev",
    "dev:web": "yarn workspace web dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true
}
